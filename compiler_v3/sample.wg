Int o = 1 + 2 * (3 * 4 + (5 + 6 * (7 + 8)));
Float p = o * 2 * 3;
add(Int a, Int b):Int {
	return a + b;
}
Int q = add(1, 2);
Int r = add(1, 2):(return);
addsub(Int a, Int b):(Int sum, Int difference) {
	sum = a + b;
	difference = a - b;
}
Int s = addsub(q, r):(return, Int t);

Int inc = 0;
for (Int i of {0, 10, 5}, {0.01, 30.02, 3}) { 
	inc = inc + i;
}
